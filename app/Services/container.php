<?php

namespace App\Services;
use App\Http\Controllers\InhouseController;

class container
{
    /**
     * 定義された文字列を返す
     *
     * @return string
     */
    public function serch($query)
    {
        // $controller = new InhouseController();
        // return $controller->result($query);
        // $controller->serch($query);
        // $result = $Inhous->simpleSerch($query);
        // view('result');
    }
}